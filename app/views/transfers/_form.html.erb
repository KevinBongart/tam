<%= form_with(model: transfer, local: true) do |form| %>
  <% if transfer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transfer.errors.count, "error") %> prohibited this transfer from being saved:</h2>

      <ul>
        <% transfer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :from_stop_id %>
    <%= form.number_field :from_stop_id %>
  </div>

  <div class="field">
    <%= form.label :to_stop_id %>
    <%= form.number_field :to_stop_id %>
  </div>

  <div class="field">
    <%= form.label :transfer_type %>
    <%= form.text_field :transfer_type %>
  </div>

  <div class="field">
    <%= form.label :min_transfer_time %>
    <%= form.number_field :min_transfer_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
