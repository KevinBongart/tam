<%= form_with(model: route, local: true) do |form| %>
  <% if route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
        <% route.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tam_id %>
    <%= form.text_field :tam_id %>
  </div>

  <div class="field">
    <%= form.label :short_name %>
    <%= form.text_field :short_name %>
  </div>

  <div class="field">
    <%= form.label :long_name %>
    <%= form.text_field :long_name %>
  </div>

  <div class="field">
    <%= form.label :route_type %>
    <%= form.number_field :route_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
